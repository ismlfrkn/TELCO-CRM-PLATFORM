spring:
  cloud:
    config:
      enabled: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate

eureka:
  client:
    enabled: false

gateway:
  internal-secret: test-secret
