server:
  port: 9006

spring:
  application:
    name: usage-service
  datasource:
    url: jdbc:postgresql://localhost:5433/usage_db
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
  cloud:
    function:
      definition: cdrEvents
    stream:
      kafka:
        binder:
          brokers: localhost:9092
      bindings:
        cdrEvents-in-0:
          destination: telco.cdr.events
          group: usage-service
          content-type: application/json
        usageEvents-out-0:
          destination: telco.usage.events
          content-type: application/json

outbox:
  publisher:
    poll-interval-ms: 5000
    batch-size: 20

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
