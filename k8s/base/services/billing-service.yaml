apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: telco-crm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until wget -q -O- http://discovery-server:8761/actuator/health 2>/dev/null | grep -q '"status":"UP"'; do echo "waiting for discovery-server..."; sleep 3; done
              until wget -q -O- http://config-server:8888/actuator/health 2>/dev/null | grep -q '"status":"UP"'; do echo "waiting for config-server..."; sleep 3; done
              until nc -z postgres 5432; do echo "waiting for postgres..."; sleep 3; done
              until nc -z kafka 29092; do echo "waiting for kafka..."; sleep 3; done
      containers:
        - name: billing-service
          image: telco-billing-service:latest
          ports:
            - containerPort: 9007
          envFrom:
            - secretRef: { name: telco-secrets }
          env:
            # PDFBox fatura uretimi diger servislere gore biraz daha fazla heap ister (bkz. docker-compose.yml).
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms96m -Xmx352m" }
            - { name: SPRING_CONFIG_IMPORT, value: "optional:configserver:http://config-server:8888" }
            - { name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE, value: "http://discovery-server:8761/eureka/" }
            - { name: SPRING_DATASOURCE_URL, value: "jdbc:postgresql://postgres:5432/billing_db" }
            - { name: SPRING_CLOUD_STREAM_KAFKA_BINDER_BROKERS, value: "kafka:29092" }
          resources:
            requests: { memory: "352Mi", cpu: "100m" }
            limits: { memory: "512Mi", cpu: "500m" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 9007 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /actuator/health, port: 9007 }
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health, port: 9007 }
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: telco-crm
spec:
  selector:
    app: billing-service
  ports:
    - port: 9007
      targetPort: 9007
